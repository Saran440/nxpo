<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="view_procurement_type_tree" model="ir.ui.view">
        <field name="model">procurement.type</field>
        <field name="arch" type="xml">
            <tree string="Procurement Types" decoration-muted="active == False">
                <field name="sequence" widget="handle" />
                <field name="active" invisible="True" />
                <field name="code" />
                <field name="name" />
                <field name="description" />
            </tree>
        </field>
    </record>
    <record id="view_procurement_type_form" model="ir.ui.view">
        <field name="model">procurement.type</field>
        <field name="arch" type="xml">
            <form string="Type">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button
                            name="toggle_active"
                            type="object"
                            groups="base.group_user"
                            class="oe_stat_button"
                            icon="fa-archive"
                        >
                            <field
                                name="active"
                                widget="boolean_button"
                                options='{"terminology": "archive"}'
                            />
                        </button>
                    </div>
                    <div class="oe_title">
                        <label for="name" class="oe_edit_only" />
                        <h1>
                            <field name="name" />
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="code" />
                        </group>
                    </group>
                    <group string="Description">
                        <field
                            name="description"
                            nolabel="1"
                            placeholder="Add an internal description that will be reason on the procurement type"
                        />
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <record model="ir.actions.act_window" id="action_procurement_type_view">
        <field name="name">Procurement Types</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">procurement.type</field>
        <field name="view_mode">form,tree</field>
        <field name="view_id" ref="view_procurement_type_tree" />
    </record>

    <!-- menu -->
    <menuitem
        id="menu_procurement_type"
        parent="purchase.menu_purchase_config"
        sequence="60"
        action="action_procurement_type_view"
        groups="purchase.group_purchase_user,purchase.group_purchase_manager"
    />
</odoo>
