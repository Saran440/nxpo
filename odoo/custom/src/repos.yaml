# See https://github.com/Tecnativa/doodba#optodoocustomsrcreposyaml
./odoo:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    ocb: https://github.com/OCA/OCB.git
    # odoo: https://github.com/odoo/odoo.git
    # openupgrade: https://github.com/OCA/OpenUpgrade.git
  target: ocb $ODOO_VERSION
  merges:
    - ocb $ODOO_VERSION
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/1731/head

web:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/web.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/1731/head # web_widget_x2many_2d_matrix

sale-workflow:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/sale-workflow.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

purchase-workflow:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/purchase-workflow.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/1023/head # purchase_open_qty
    - origin refs/pull/1016/head # purchase_isolated_rfq
    - origin refs/pull/1021/head # purchase_work_acceptance_evaluation
    - origin refs/pull/1020/head # purchase_work_acceptance_late_fines
    - origin refs/pull/1025/head # purchase_work_acceptance_invoice_plan
    - origin refs/pull/1079/head # purchase_rfq_number
    - origin refs/pull/1072/head # purchase_request_type

account-invoicing:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/account-invoicing.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/812/head # account_billing
    - origin refs/pull/820/head # account_invoice_payment_retention
    - origin refs/pull/821/head # account_invoice_payment_warranty

account-financial-tools:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/account-financial-tools.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/1112/head # account_asset_management
    - origin refs/pull/1082/head # account_check_deposit
    - origin refs/pull/1081/head # account_fiscal_year

account-financial-reporting:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/account-financial-reporting.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

operating-unit:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/operating-unit.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/332/head # account_operating_unit
    - origin refs/pull/346/head # hr_expense_operating_unit
    - origin refs/pull/343/head # purchase_operating_unit
    - origin refs/pull/348/head # purchase_request_operating_unit
    - origin refs/pull/344/head # stock_operating_unit

bank-payment:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/bank-payment.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

account-payment:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/account-payment.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/374/head # account_payment_multi_deduction

account-analytic:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/account-analytic.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/339/head # analytic_base_department

server-ux:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/server-ux.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/246/head # base_revision
    - origin refs/pull/228/head # base_tier_validation_server_action

server-tools:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/server-tools.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

server-backend:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/server-backend.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

budgeting:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/ecosoft-odoo/budgeting.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION # all budget management modules
    - origin refs/pull/4/head # updated all budget

hr-expense:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/hr-expense.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/25/head # hr_expense_advance_clearing
    - origin refs/pull/26/head # hr_expense_advance_clearing_detail

l10n-thailand:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/l10n-thailand.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/220/head # l10n_th_base_location
    - origin refs/pull/206/head # l10n_th_withholding_tax_cert
    - origin refs/pull/192/head # l10n_th_withholding_tax_cert_form
    - origin refs/pull/209/head # l10n_th_withholding_tax_multi
    - origin refs/pull/214/head # l10n_th_withholding_tax_report

mis-builder:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/mis-builder.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/325/head # mis_builder, mis_builder_budget, mis_builder_demo

partner-contact:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/partner-contact.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/1018/head # partner_industry_secondary

stock-logistics-warehouse:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/stock-logistics-warehouse.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/985/head # stock_request
    - origin refs/pull/1014/head # stock_request_tier_validation

stock-logistics-workflow:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/stock-logistics-workflow.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/738/head # stock_return_request
